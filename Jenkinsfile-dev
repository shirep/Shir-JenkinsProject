pipeline {
    agent any
    environment {DOCKERHUB_CREDENTIALS= credentials('dockerhub')}
    stages {
        stage('Checkout') {
            steps {
                script {
                    checkout scm
                }
            }
        }
        stage('Docker Build Shir Python Image') {
            steps {
                script {
                    sh 'docker build -t shir-python-image:$BUILD_NUMBER .'
                    echo 'Build Completed' 
                }
            }
        }

        stage('Login to Docker Hub') {         
            steps{
		script {
		    sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'                 
	            echo 'Login Completed'                
                }           
            } 
        }  

        stage('Push Image to Docker Hub') {         
            steps{
		script {
		    sh 'docker tag shir-python-image:$BUILD_NUMBER shirep/shir-python-image:$BUILD_NUMBER'
                    sh 'docker tag shir-python-image:$BUILD_NUMBER shirep/shir-python-image:latest'
                    sh 'docker push shirep/shir-python-image:$BUILD_NUMBER'
                    sh 'docker push shirep/shir-python-image:latest'                 
                    echo 'Push Image Completed'        
                }           
            } 
        }

        stage('Push Image to Docker Hub') {  
            steps{
		script {
                    sh 'docker run -itd --name shir-container shirep/shir-python-image:$BUILD_NUMBER'

  }

    post {
        always {  
            sh 'docker logout'           
        } 
        success {
            echo 'Success'
        }
        failure {
            echo 'Failed'
        }
    }
}
